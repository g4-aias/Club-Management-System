<div id = "posts" class = "transitions-enabled" >

  <% @posts.each do |post| %>
  <% @post = post %>
  
    <%= render 'posts/heading' %>
    
  <% end %>
  <br>
</div>

<%= will_paginate @posts %>

